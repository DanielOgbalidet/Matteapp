<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/math_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/math_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mappe1"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SpillActivity"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".PreferanserActivity" />
        <activity android:name=".OmSpillActivity" />

    </application>

</manifest>

<!-- android:configChanges="orientation|screenSize" -->